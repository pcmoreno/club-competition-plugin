# Composer
/vendor/

# Local development
dev/
Makefile

# Node
/node_modules/

# build/ is intentionally committed: SiteGround has no Node.js, so the
# compiled frontend must be built locally and shipped via git. See CLAUDE.md.

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
*.iml
.phpstorm.meta.php
_ide_helper.php

# KNSB rating data — fetched at runtime (wp scs fetch-knsb-ratings) into
# uploads/, never the plugin dir. This entry stays so a list left behind by a
# pre-0.4.0 install (KnsbRatingStore migrates it on first use) can't be
# committed by accident.
/resources/KnsbRatings/*.json

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
.env.*.local

# PHP CS Fixer
.php-cs-fixer.cache

# Logs
*.log

# WordPress
wp-config.php
/wp-content/
/wp-admin/
/wp-includes/

# Testing
.phpunit.cache/
coverage/

# Release artifacts
/dist/
